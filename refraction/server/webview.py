# This module has been removed.  The desktop app is now a native SwiftUI
# application (RefractionApp/) that communicates with the FastAPI server
# via the /analyze endpoint.  The legacy web-view wrapper is no longer used.
